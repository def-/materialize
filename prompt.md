We are looking through the code base in Materialize src/. Keep note of the areas you have already covered in @prompt.md. Review code to find architectural problems causing us to have scaling problems. We care about code simplicity and reuse.

We have a log file in log.md where we keep recording findings, append your results for each commit there.

In each session attempt to find one architectural problem, analyze it, think about where it causes scaling issues, note down the results in our log file and stop! I'll start a new session to try and tackle the next thing. After having analyzed bugs in a commit and noted down the results in our log.
